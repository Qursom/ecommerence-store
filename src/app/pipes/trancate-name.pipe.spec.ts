import { TrancateNamePipe } from './trancate-name.pipe';

describe('TrancateNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TrancateNamePipe();
    expect(pipe).toBeTruthy();
  });
});
